# yaml-language-server: $schema=https://json-schema.axonivy.com/app/12.0.0/variables.json
Variables:
  ibmMQ:
    # Skip MQ Listener flag: When true, prevents automatic IBM MQ listener initialization at application startup (default: false)
    skipListener: "false"
